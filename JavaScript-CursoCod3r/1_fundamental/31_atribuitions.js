const a = 4;
let b = 6;

b += a; // (b = b + a)
console.log(b);

b -= a; // (b = b - a)
console.log(b);

b *= a; // (b = b * a)
console.log(b);

b /= a; // (b = b / a)
console.log(b);

b %= 2; // (b = b % 2)
console.log(b);