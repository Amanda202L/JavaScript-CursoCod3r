// Now, this simple code is only to show you up a bit about 'Math'.

const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);

console.log(area, "or", area.toFixed(2));
console.log("Math's typeof is it: ", typeof Math);